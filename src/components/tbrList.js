import React from "react";
import BookCard from './BookCard';

export default function TBR({ books }) {
    //TODO filter for ReadStatus: TBR, map onto BookCard
    
    return (
        <div>
            
        </div>
    )
}